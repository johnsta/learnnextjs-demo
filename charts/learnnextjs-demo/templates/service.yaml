apiVersion: v1
kind: Service
metadata:
  name: {{ template "learnnextjs-demo.fullname" . }}
  labels:
    app: {{ template "learnnextjs-demo.name" . }}
    chart: {{ template "learnnextjs-demo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "learnnextjs-demo.name" . }}
    release: {{ .Release.Name }}
